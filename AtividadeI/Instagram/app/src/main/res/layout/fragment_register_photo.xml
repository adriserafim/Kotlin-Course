<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="70dp">

            <ImageView
                android:layout_width="@dimen/register_photo_spot_width"
                android:layout_height="@dimen/register_photo_spot_height"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="@dimen/register_photo_spot_margin_bottom"
                android:contentDescription="@string/register_photo_spot"
                android:src="@drawable/photo_icon" />
            
            <TextView
                android:textSize="24sp"
                android:textColor="@color/black"
                android:layout_gravity="center"
                style="@style/Theme.Instagram.TextViewBase.Form"
                android:text="@string/photo_register_title"/>

            <TextView
                android:textColor="@color/black"
                android:layout_gravity="center"
                style="@style/Theme.Instagram.TextViewBase.Form"
                android:text="@string/photo_register_subtitle"/>

            <co.tiagoaguiar.course.instagram.login.view.LoadingButton
                android:id="@+id/register_btn_next"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:autofillHints="@string/next"
                app:text="@string/next" />

        </LinearLayout>

    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>